# Local development configuration for Phase 2.
# Copy this file to local_config.yaml if you want machine-specific overrides.

data_lake_root: "./data_lake"
bronze_path: "./data_lake/bronze"
silver_path: "./data_lake/silver"
gold_path: "./data_lake/gold"

sql_server:
  driver: "ODBC Driver 18 for SQL Server"
  server: "localhost"
  database: "EnergyWarehouse"
  trusted_connection: true
  username: ""
  password: ""
  encrypt: true
  trust_server_certificate: true
